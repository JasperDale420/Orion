## Verdict: **DO NOT SHIP** any factor gate, tilt, or claimed Orion expectancy improvement from this study.

C1’s action—do not deploy a gate—is right. Several reasons offered for it are not yet valid evidence about Orion.

| Severity | Finding | Required disposition |
|---|---|---|
| **CRITICAL** | **Features are available after the label’s claimed entry time.** The watch resolves an entry price, creates the watch, then extracts features; the feature record explicitly stamps `ts_available=datetime.now()` after the event. A positive 12.7s lag proves this. Predicting return measured from the alert-time entry with information observed later creates an execution-time mismatch and can leak the first post-alert move. [consumer.py](/Users/jacobmcmillan/Empire/Heber/heber/watch/consumer.py:624) [consumer.py](/Users/jacobmcmillan/Empire/Heber/heber/watch/consumer.py:1355) | Rebuild labels from a decision-time quote at or after feature availability, and measure return only after that timestamp. Until then, all factor effects—including “nulls”—are invalid for live use. |
| **CRITICAL** | **The label is not an Orion P&L proxy.** Heber enters through a heterogeneous fallback chain: sweep trade price, later live mid, payload mid, last, then an unflagged `$1` floor; it exits on a mid-based `+29%/-15%` watch barrier. Orion has different barriers, fresh-bid marketable limit exits, and additional expiry/no-progress/drawdown rules. Shifting TP payouts by 4pp cannot repair different stopping times. [consumer.py](/Users/jacobmcmillan/Empire/Heber/heber/watch/consumer.py:1082) [exit_fallback_rules.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/execution/exit_fallback_rules.py:458) [execution_engine.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/execution/execution_engine.py:2658) | Withdraw any claimed Orion gross/net expectancy, gate expectancy, and stop counterfactual. At most call them “Heber-label simulations.” |
| **HIGH** | **P1 is not `orion_strict`.** The actual rule also rejects known deltas outside 0.25–0.60, known low contract volume, stale signals, missing timestamps, and out-of-window ET entries. Those filters are absent from the stated funnel. [flow_rules.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/processing/rules/flow_rules.py:194) [flow_rules.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/processing/rules/flow_rules.py:209) | Rename P1 immediately; rebuild the target population from persisted Orion decision eligibility. |
| **HIGH** | **DTE is structurally confounded with the label horizon.** Heber changes from 4-hour/5-minute monitoring at DTE ≤2 to 120 trading-hours/15-minute monitoring at DTE 3–21. Orion instead groups 1–3 DTE together and uses 54 wall-clock hours, while 4–14 uses 168 hours. DTE is therefore predicting different label machinery, not demonstrated option behavior. | Withdraw C2’s “short-dated percent volatility” mechanism and its “robust predictor” language. “Do not gate on DTE” remains prudent. |
| **HIGH** | **The purported OOS model is contaminated.** Selecting its six features using full-sample p-values uses the test period to choose the model; that is not “mildly optimistic.” Also purge the final maximum label horizon from training: outcomes for late train alerts resolve after the decision cutoff. The reported AUC, top-quintile hit rate, and “best gate” cannot be cited. | Discard joint-model and best-gate OOS claims; refit all selection, binning, signs, and model tuning inside train-only folds, with maturity purge/embargo and an untouched final holdout. |
| **HIGH** | **`ts_available` is not enough for the daily tables.** A batch-write clock says when a cache row was written, not the data cutoff or first observable vendor timestamp. `ensure_ts_available` literally assigns `datetime.now()` when absent. `merge_asof` is safe only if that field is immutable, truthful availability provenance. `keep-last` over up to 86 duplicates can additionally select a later revision unless equivalence and original availability are proven. [base.py](/Users/jacobmcmillan/Empire/Heber/heber/features/pipelines/base.py:22) | Treat all daily-factor results as PIT-unverified. Preserve source cutoff, feature computation time, and immutable first availability; prove duplicate payload equality or version rows. |
| **HIGH** | **Survivorship is material and direction unknown.** Excluding 431/1,636 otherwise eligible rows conditions on a broken label process; losing all 0DTE makes the primary population nonrepresentative. You cannot infer the sign of the bias without comparing retained versus dropped rows by DTE, outcome inputs, time, ticker, spread, and factors. | No conclusion about 0DTE, and no “Orion-shaped universe” claim. Fix/relabel dead rows or present worst-/best-case bounds. |
| **HIGH** | **C6 overreaches.** Orion’s monitor defaults to 60 seconds and only tightens to 30 while any 0DTE is open; the study contains zero 0DTE. A monitor check is also not a fill: it still needs quote acquisition, cancellation, a limit submission, and may defer. Most importantly, Orion’s relevant stops are −30/−35/−40%, not Heber’s −15%. [position_monitor.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/execution/position_monitor.py:1383) [position_monitor.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/execution/position_monitor.py:1442) | Withdraw “30 seconds recovers the 9pp overshoot” and the +0.70% exact-−15% counterfactual as relevant to Orion. Keep only: exit quote/order/fill capture is necessary to measure it. |
| **HIGH** | **The cost number is a conditional quote simulation, not realized execution cost.** The algebra is correct for a mid entry plus quarter-half-spread pay-up and bid exit, but the live code rounds limits, may use fallback marks, and exits with a marketable limit. Missing `entry_quote` on 551/2,052 EXECUTE decisions is likely selection, not harmless missingness. [execution_engine.py](/Users/jacobmcmillan/Empire/Orion/.claude/worktrees/orion-debug-2ab92b/src/orion/execution/execution_engine.py:1140) | Relabel as a conditional spread scenario. Audit missingness and use realized entry/exit fills, fill rates, cancellations, and adverse selection before asserting −3.39%. |
| **MEDIUM** | Five nested populations are not five replications. They reuse most of the same alerts and the same Feb–Mar regime. Sign consistency is sensitivity, not confirmation. | Remove “all five populations” as corroboration. |
| **MEDIUM** | BH is not automatically anti-conservative merely because endpoints are correlated or duplicated; under positive dependence it can still control FDR. But identical proxy tests must be removed, and BH over 161 does not cover the actual research family: alternate populations, months, OOS gates, model selection, and post-hoc “best” selection. | For a deployment decision, use a train-only, day-blocked max-statistic/Romano–Wolf correction over the entire gate-selection workflow, then a locked final holdout. BH is fine only for explicitly exploratory factor screening. |
| **MEDIUM** | Day-cluster bootstrap has only 67 nominal clusters and ignores persistent ticker/regime dependence and overlapping holding periods. The 70% Feb–Mar concentration means this is one short, nonstationary episode, not a durable OOS demonstration. | Report date/ticker concentration, use contiguous-date blocks plus maturity embargo, and call this a pilot. |
| **MEDIUM** | “Null everywhere” is an overclaim. Your stated minimum detectable hit-rate spread is ~11pp, while literature-scale effects are 1–4pp; missing daily-factor coverage further changes each factor’s population. | C4 must become “no detectable effect of deployable size in this sample and proxy,” not “the predictors are null.” |

Claim disposition:

- **C1 — retain, narrower.** “No study-supported gate or size tilt should be deployed.”
- **C2 — retain the no-gate decision; withdraw the mechanical explanation and robustness claim.**
- **C3 — withdraw.** `f_vol_oi` and `f_abs_delta` are exploratory observations, not shadow candidates.
- **C4 — qualify.** Absence of detection is not evidence of nullity here.
- **C5 — withdraw the causal diagnosis.** Replace with: “The Heber-label cost simulation is negative; no noisy conditional estimate supports deployment.”
- **C6 — retain measurement priority, withdraw the quantified Orion benefit.** The next measurement bundle must capture decision availability time, accepted/rejected entry orders, entry fills, exit trigger time, quote, order, fill, and completion latency.

The most defensible record today is: *this pilot has found no admissible evidence to reduce trade count or tilt size; it has not established that ranking is useless, that Orion expectancy is −3.4%, or that faster monitoring would recover 4.1pp.*
